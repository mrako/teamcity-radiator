#container.variable-sizes
  - @projects.each do |p|
    - p[:types].each do |t|
      .element.width2{class: t[:status]}
        .number= t[:name]
        %h3.symbol= p[:name]
        %h2.name= p[:name]
        %p.weight
